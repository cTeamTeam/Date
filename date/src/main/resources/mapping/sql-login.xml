<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">
	
<!-- 로그인 -->

	<select id="checkId" parameterType="String" resultType="loginVo">
		select id, password, nickname from member where password = #{password}
	</select>
	
	<!-- 아이디 찾기 -->
	<select id="forGot" parameterType="LoginVo" resultType="loginVo">
		select name, phonenum , id from member where name = #{name} and phonenum = #{phoneNum}
	</select>
	
	<!-- 비밀번호 찾기 -->
	<select id="forGotPw" parameterType="LoginVo" resultType="loginVo">
		select password, phonenum , id from member where id = #{id} and phonenum = #{phoneNum}
	</select>
	
	<select id="pwTranslate" parameterType="String" resultType="String">
		select password from member where id=#{id}
	</select>
	
	
	<!--  마이페이지 테스트 -->
	<select id="list" resultType="loginVo">
		SELECT ID, NAME, NICKNAME,BIRTH,GENDER,EMAIL, PHONENUM, MEMBERSHIP, DATE_COUNT FROM MEMBER WHERE ID= #{id}
	</select>
	<!--  회원 탈퇴 테스트 -->
	<select id="withdraw" resultType="int">
		UPDATE MEMBER SET SESSION_STATE=1 WHERE ID='tester1234'
	</select>
</mapper>